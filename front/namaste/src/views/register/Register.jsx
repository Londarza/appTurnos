
import RegisterForm from '../../components/registerForm/RegisterForm.jsx';
import { RegisterContainer, Title } from './register.js';

const Register = () => {
  return (
    <RegisterContainer>
      <Title>Registro</Title>
      <RegisterForm />
    </RegisterContainer>
  );
};

export default Register;
